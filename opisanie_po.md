# Система управления автопарком предприятия

## Общее описание

Веб-система для управления автопарком предприятия с функциями учёта транспортных средств, планирования технического обслуживания (ТО) и прогнозирования сроков следующего ТО на основе анализа пробега.

---

## Технологический стек

### Backend (серверная часть)
- **Python 3.11+** — основной язык программирования
- **Flask 3.0** — веб-фреймворк для создания REST API и рендеринга страниц
- **SQLAlchemy 2.0** — ORM для работы с базой данных
- **Flask-JWT-Extended** — JWT-аутентификация и авторизация
- **Flask-CORS** — поддержка кросс-доменных запросов
- **Werkzeug** — хеширование паролей

### База данных
- **SQLite** — встроенная база данных (по умолчанию для локальной разработки)
- **PostgreSQL** — поддерживается для продакшн-окружения

### Frontend (клиентская часть)
- **HTML5** — разметка страниц
- **CSS3** — стилизация (адаптивный дизайн, media queries)
- **JavaScript (ES6+)** — динамическое взаимодействие с API
- **Jinja2** — шаблонизатор для серверного рендеринга

---

## Структура проекта

```
car/
├── app/
│   ├── __init__.py          # Инициализация Flask-приложения
│   ├── models.py            # Модели базы данных (SQLAlchemy)
│   ├── routes.py            # API и веб-маршруты
│   ├── utils.py             # Вспомогательные функции
│   └── templates/           # HTML-шаблоны
│       ├── base.html        # Базовый шаблон
│       ├── login.html       # Страница входа
│       ├── vehicles.html    # Реестр транспорта
│       ├── vehicle_detail.html  # Детали ТС
│       ├── maintenance.html # Календарь ТО
│       ├── predictions.html # Прогнозы ТО
│       └── notifications.html   # Уведомления
├── config.py                # Конфигурация приложения
├── run.py                   # Точка входа
├── seed_data.py             # Наполнение тестовыми данными
├── requirements.txt         # Зависимости Python
└── fleet.db                 # База данных SQLite
```

---

## Модели данных

### User (Пользователь)
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| username | String(80) | Логин (уникальный) |
| email | String(120) | Email (уникальный) |
| password_hash | String(256) | Хеш пароля |
| role | Enum | Роль: admin, mechanic, driver |
| full_name | String(200) | ФИО |
| created_at | DateTime | Дата создания |

### Vehicle (Транспортное средство)
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| brand | String(100) | Марка |
| model | String(100) | Модель |
| year | Integer | Год выпуска |
| vin | String(17) | VIN-код (уникальный) |
| reg_number | String(20) | Гос. номер (уникальный) |
| purchase_date | Date | Дата покупки |
| initial_mileage | Integer | Начальный пробег |
| current_mileage | Integer | Текущий пробег |
| vehicle_type | Enum | Тип: car, truck, bus, special |
| status | String | Статус: active, maintenance, repair, inactive |

### Maintenance (Техническое обслуживание)
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| vehicle_id | Integer | FK на Vehicle |
| type | Enum | Тип ТО: regular, oil_change, tire_change, brake_service, inspection, other |
| date | Date | Дата проведения |
| mileage | Integer | Пробег на момент ТО |
| cost | Decimal | Стоимость |
| description | Text | Описание работ |
| next_maintenance_km | Integer | Пробег для следующего ТО |

### Repair (Ремонт)
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| vehicle_id | Integer | FK на Vehicle |
| description | Text | Описание поломки |
| repair_date | Date | Дата ремонта |
| cost | Decimal | Стоимость |
| parts_replaced | Text | Заменённые детали |
| status | Enum | Статус: pending, in_progress, completed |

### MileageLog (Журнал пробега)
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Первичный ключ |
| vehicle_id | Integer | FK на Vehicle |
| date | Date | Дата записи |
| mileage | Integer | Показание одометра |
| recorded_by | Integer | FK на User |

---

## Система ролей и прав доступа

### Администратор (admin)
- Полный доступ ко всем функциям
- Управление пользователями
- Добавление, редактирование, удаление ТС
- Управление записями ТО
- Просмотр всех данных

### Механик (mechanic)
- Добавление и редактирование ТС
- Создание и редактирование записей ТО
- Просмотр всех транспортных средств
- Не может удалять данные

### Водитель (driver)
- Только просмотр данных
- Видит список ТС и их детали
- Видит историю ТО
- Не может редактировать данные

---

## API Endpoints

### Аутентификация
| Метод | Путь | Описание |
|-------|------|----------|
| POST | /api/login | Вход в систему, получение JWT-токена |

### Транспортные средства
| Метод | Путь | Описание |
|-------|------|----------|
| GET | /api/vehicles | Список всех ТС |
| POST | /api/vehicles | Создание нового ТС |
| GET | /api/vehicles/{id} | Детали ТС |
| PUT | /api/vehicles/{id} | Обновление ТС |
| DELETE | /api/vehicles/{id} | Удаление ТС |

### Техническое обслуживание
| Метод | Путь | Описание |
|-------|------|----------|
| GET | /api/vehicles/{id}/maintenance | История ТО для ТС |
| GET | /api/maintenance/all | Все записи ТО |
| GET | /api/maintenance/upcoming | Предстоящие ТО (прогноз) |
| POST | /api/maintenance | Создание записи ТО |
| PUT | /api/maintenance/{id} | Обновление записи ТО |
| DELETE | /api/maintenance/{id} | Удаление записи ТО |

### Прогнозирование
| Метод | Путь | Описание |
|-------|------|----------|
| GET | /api/predictions | Прогнозы ТО для всех ТС |

### Уведомления
| Метод | Путь | Описание |
|-------|------|----------|
| GET | /api/notifications | Список уведомлений |

---

## Алгоритм прогнозирования ТО

Система рассчитывает дату следующего ТО на основе:

1. **Среднесуточный пробег** — вычисляется по истории записей пробега
2. **Интервал ТО** — стандартный интервал 10 000 км (или указанный в последнем ТО)
3. **Текущий пробег** — актуальные показания одометра

**Формула:**
```
Оставшийся пробег = Целевой пробег - Текущий пробег
Дней до ТО = Оставшийся пробег / Среднесуточный пробег
Дата ТО = Сегодня + Дней до ТО
```

**Статусы прогноза:**
- 🟢 **Норма** — более 30 дней до ТО
- 🟡 **Скоро** — от 7 до 30 дней
- 🔴 **Срочно** — менее 7 дней
- ⚫ **Просрочено** — ТО уже должно было быть проведено

---

## Веб-страницы

### Вход в систему (/login)
- Форма авторизации с логином и паролем
- JWT-токен сохраняется в localStorage
- Автоматическое перенаправление авторизованных пользователей

### Транспорт (/vehicles)
- Таблица всех транспортных средств
- Фильтрация и поиск
- Кнопки добавления/редактирования (для admin/mechanic)
- Переход к детальной информации

### Детали ТС (/vehicles/{id})
- Полная информация о транспортном средстве
- История технического обслуживания
- Кнопки редактирования ТС и добавления ТО (для admin/mechanic)

### Календарь ТО (/maintenance)
- Все записи о проведённом ТО
- Информация о ТС, типе ТО, пробеге, стоимости
- Статус транспортного средства
- Кнопка добавления нового ТО (для admin/mechanic)

### Прогнозы (/predictions)
- Предстоящие ТО с датами и статусами
- Сортировка по срочности
- Цветовая индикация статуса

### Уведомления (/notifications)
- Системные уведомления о предстоящих и просроченных ТО
- Фильтрация по типу и статусу

---

## Адаптивный дизайн

Система полностью адаптирована для мобильных устройств:

- **Desktop (> 768px)** — стандартные таблицы
- **Mobile (< 768px)** — таблицы преобразуются в карточки
- **Навигация** — мобильное меню (гамбургер)
- **Модальные окна** — адаптивная ширина

---

## Безопасность

- **JWT-аутентификация** — токены с ограниченным сроком действия
- **Хеширование паролей** — Werkzeug security
- **Проверка ролей** — на уровне API и UI
- **Валидация VIN** — проверка контрольной суммы
- **CORS** — настраиваемые разрешённые источники

---

## Установка и запуск

### Требования
- Python 3.9+
- pip

### Установка
```bash
# Клонирование репозитория
cd car

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Наполнение тестовыми данными
python seed_data.py

# Запуск
python run.py
```

### Доступ
- URL: http://localhost:5001
- Администратор: `admin` / `password123`
- Механик: `mechanic` / `password123`
- Водитель: `driver` / `password123`

---

## Конфигурация

Файл `config.py` содержит настройки:

| Параметр | Описание |
|----------|----------|
| SECRET_KEY | Секретный ключ Flask |
| SQLALCHEMY_DATABASE_URI | Строка подключения к БД |
| JWT_SECRET_KEY | Секретный ключ для JWT |
| JWT_ACCESS_TOKEN_EXPIRES | Время жизни токена (24 часа) |

Переменные окружения:
- `DATABASE_URL` — строка подключения к PostgreSQL
- `SECRET_KEY` — секретный ключ приложения

---

## Зависимости (requirements.txt)

```
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==4.0.0
SQLAlchemy==2.0.23
Werkzeug==3.0.1
python-dotenv==1.0.0
```

---

Лицензия

Проект разработан для внутреннего использования предприятия.


